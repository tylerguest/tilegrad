# TileGrad specification

`tilegrad-spec.tex` is the normative north-star specification for TileGrad V1.
It intentionally describes the intended stable language rather than the current
experimental implementation.

Render the committed PDF after changing the source:

```bash
./render.sh
```

The script uses `tectonic` when available and otherwise falls back to
`pdflatex`.
